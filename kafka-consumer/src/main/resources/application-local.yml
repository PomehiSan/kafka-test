spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: 'http://${eureka.instance.hostname}:8761/eureka'
